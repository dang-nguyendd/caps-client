import { SelectOption, SelectOptions } from "@/core/select-option/type";

export const CheckboxOptions: SelectOptions = [
  { label: "Yes", value: "yes" },
  { label: "No", value: "no" },
];

export const DefaultCheckboxOption: SelectOption = CheckboxOptions[0];
