export type LoginForm = {
    email: string;
    password: string;
}

export type RegisterForm = {
    email: string;
    password: string;
    name: string;
    dob: string;
    gender: string;
    confirmPassword: string
}